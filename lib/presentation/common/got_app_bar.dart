import 'package:flutter/widgets.dart';

class GotAppBar extends StatelessWidget {
  @override
  Widget build(BuildContext context) => Text(
        'An App of Ice And Fire',
        style: TextStyle(fontSize: 16),
      );
}
